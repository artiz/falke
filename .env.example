# === Falke Trading Bot Configuration ===

# Trading mode: "paper" (default) or "live"
TRADING_MODE=paper

# Paper trading initial balance (USD)
PAPER_BALANCE=1000.0

# === Telegram ===
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Comma-separated phone numbers allowed to register (with country code)
ALLOWED_PHONES=+43xxxxxxxxxx

# === Polymarket ===
# Gamma API (public, no auth needed)
GAMMA_API_URL=https://gamma-api.polymarket.com
# CLOB API (requires wallet signature)
CLOB_API_URL=https://clob.polymarket.com

# === Wallet (for live trading only) ===
# WARNING: Keep this secret! In production, use AWS Secrets Manager instead.
WALLET_PRIVATE_KEY=

# === Polymarket Relayer API Keys (for live trading) ===
# Get from: Polymarket UI → Profile → API Keys
RELAYER_API_KEY=
RELAYER_API_KEY_ADDRESS=

# === Testing Mode ===
# Set to "true" to run 10×10×10 = 1000 parameter sweep portfolios in parallel.
# Use /test in Telegram to see the top-10 leaderboard.
TESTING_MODE=false

# === Strategy: Tail Risk ===
# Only trade outcomes priced at or below this threshold (e.g. 0.03 = 3 cents)
TAIL_RISK_MAX_PRICE=0.045
# Minimum bet size per position (USD)
TAIL_RISK_BET_USD=5.0
# Edge multiplier: how much we assume market underprices tail events (2.0 = true prob is 2x market price)
TAIL_RISK_KELLY_EDGE_MULTIPLIER=2.0
# Minimum payout multiplier to enter (25.0 = only take outcomes paying 25x or more, i.e. price <= 4c)
TAIL_RISK_MIN_PAYOUT_MULTIPLIER=25.0
# Fraction of positions assigned a take-profit exit (0.5 = 50% use TP, 50% hold to resolution)
TAIL_RISK_TAKE_PROFIT_FRACTION=0.5
# Take profit threshold (% gain)
TAIL_RISK_TAKE_PROFIT_PCT=30.0
# Stop loss threshold (% loss, 0.0 = disabled)
TAIL_RISK_STOP_LOSS_PCT=0.0

# Testing
TAIL_RISK_MAX_PRICE_MIN=0.03
TAIL_RISK_MAX_PRICE_MAX=0.10
TAIL_RISK_BET_USD_MIN=3.0
TAIL_RISK_BET_USD_MAX=10.0
TAIL_RISK_TAKE_PROFIT_PCT_MIN=30.0
TAIL_RISK_TAKE_PROFIT_PCT_MAX=70.0


# === Market Filters ===
MARKET_EXPIRY_WINDOW_DAYS=5
MIN_LIQUIDITY_USD=1000.0

# === Risk Management ===
TRADE_POLL_INTERVAL_SEC=10
MAX_BET_USD=50.0
MAX_OPEN_POSITIONS=100
COOLDOWN_SEC=600
PNL_NOTIFY_THRESHOLD_USD=50.0

# === DynamoDB ===
AWS_REGION=eu-west-2
# Table prefix (tables: {prefix}-dev-users, {prefix}-dev-sessions, {prefix}-dev-trades)
DYNAMO_TABLE_PREFIX=falke
# Local DynamoDB endpoint (LocalStack). Remove or leave empty for real AWS.
DYNAMO_ENDPOINT=http://localhost:4567

# === Logging ===
RUST_LOG=falke=info,teloxide=warn
